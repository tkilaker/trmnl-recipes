{% comment %}
DN News - Quadrant Layout
Iterates `items` produced by src/transform.js (post-payload-trim).
{% endcomment %}

{% assign max_height = 160 %}
{% assign show_more_label = false %}
{% assign news_limit = 3 %}

<div class="layout">
  <div class="list gap--xsmall" data-list-limit="true" data-list-max-height="{{ max_height }}" data-list-hidden-count="{{ show_more_label }}">
    {% if items.size > 0 %}
      {% for item in items limit: news_limit %}
        <div class="item gap--xsmall">
          <div class="meta">
            <span class="index"></span>
          </div>
          <div class="content stretch">
            <span class="title title--xsmall clamp--1">{{ item.title | strip_html }}</span>
          </div>
        </div>
      {% endfor %}
    {% else %}
      <div class="text--xs text--gray-3">Inga nyheter</div>
    {% endif %}
  </div>
</div>
<div class="title_bar">
  <span class="title text--xs">
    <img src="https://cdn.dn-static.se/images/favicons/32-NQBIGKWZ.png" width="14" height="14" style="display: inline-block; vertical-align: middle; margin-right: 2px;">
    DN
  </span>
</div>
